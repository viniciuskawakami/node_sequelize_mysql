// .sequelizerc
const { resolve } = require("path");

module.exports = {
  config: resolve(__dirname, "src", ".config", ".config.js"),
  "models-path": resolve(__dirname, "src", "models"),
  "seeders-path": resolve(__dirname, "src", ".config", "database", "seeders"),
  "migrations-path": resolve(__dirname, "src", "database", "migrations", "v1"),
};
